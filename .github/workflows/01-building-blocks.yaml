name: 01-building-blocks

on: push

jobs:
    first-job:
        runs-on: ubuntu-latest
        steps:
            - name: say hello
              run: echo "Hello World"
    second-job:
        runs-on: ubuntu-latest
        steps:
            - name: failing step
              run: exit 1
            - name: say goodbye
              run: echo "Goodbye World"